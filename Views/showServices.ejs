<!DOCTYPE html>
<html lang="en">
    <head>
    <link href="/css/style.css" rel="stylesheet" type="text/css">
        <title></title>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        

    </head>
    <body class="viewfood">
         <%for(let i = 0;i<categories.length;i++){%>
        <div class="category"><%=categories[i]%></div>
        <%for(let j = 0;j<foods.length;j++){
            if(foods[j].category===categories[i]){%>
            <div class="foodrow">
                <div class="foodcoli"><img class="foodimg"  src="<%=foods[j].img%>"></div>
                <div class="foodcol">
                <div class='foodname'><%=foods[j].name%></div>
                <%if(foods[j].vegetarian){%>
                    <span class="veggie">VEGETARIAN!</span>
                    <%}%><br/>
                <span class = "fooddesc"><%=foods[j].description%></span><br/>
                </div>
            </div>
            <%}%>
        <%}%>
        <hr>
    <%}%>

    

    </body>
</html>